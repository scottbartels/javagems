/**
 * A library for command line options handling. Two main types of objects are involved:
 * "options", which represents options designed by a programmer and expected by a program,
 * and "actuators", which represents recognized user inputs typed on command line.
 */
package gems.cli;
